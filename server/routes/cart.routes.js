const express = require('express');

const router = express.Router();

// Le panier est géré côté client (localStorage).
// Ce fichier est réservé pour de futures routes liées au panier si nécessaire
// (ex: vérification de stock en temps réel, application de codes promo).

module.exports = router;
